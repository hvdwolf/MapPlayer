<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">


    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />



    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:resizeableActivity="true"
        android:label="MapPlayer"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/Theme.MapPlayer" >

        <activity android:name=".ui.folder.FolderBrowserActivity" android:exported="false" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />


        <activity android:name=".ui.settings.SettingsActivity" android:exported="false" />
        <activity android:name=".ui.player.PlayerActivity" android:exported="false" />
        <activity android:name=".ui.settings.LogViewerActivity" android:exported="false" />

        <service
            android:name=".player.MusicService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            android:enabled="true" >

            <intent-filter>
                <!-- REQUIRED for MediaBrowserServiceCompat -->
                <action android:name="android.service.media.MediaBrowserService" />

                <!-- Modern Android Auto -->
                <action android:name="android.media.browse.MediaBrowserService" />

                <!-- Legacy / FYT / OEM Android Auto -->
                <action android:name="com.google.android.gms.car.media.MediaBrowserService" />

                <category android:name="android.media.category.CAR_MEDIA" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>